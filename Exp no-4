//SANTHOSH G  192521035
//recognize the operators +,-,*,/ usingregular Arithmetic operators.
#include <stdio.h>
int main() {
    char op;
    printf("Enter an operator: ");
    scanf("%c", &op);
    if (op == '+' || op == '-' || op == '*' || op == '/')
        printf("Valid Arithmetic Operator");
    else
        printf("Invalid Operator");
    return 0;
}

Input:
+
Output:
Valid Arithmetic Operator
